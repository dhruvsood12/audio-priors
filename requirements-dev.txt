# Development dependencies for audio-priors. Source of truth is pyproject.toml.
-r requirements.txt

# tests
pytest>=8.3
pytest-cov>=5.0
hypothesis>=6.115

# lint, type-check, hooks, audit
ruff>=0.7
mypy>=1.13
nbstripout>=0.7
pre-commit>=4.0
pip-audit>=2.7
pandas-stubs>=2.0

# notebooks and app (for local development)
jupyter>=1.0
ipykernel>=6.25
streamlit>=1.39
