services:
  pipeline:
    build: .
    image: songaddiction:latest
    container_name: songaddiction-pipeline
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
      - ./notebooks:/app/notebooks
